[pytest]
# Add the backend/ directory to sys.path so that `from app import ...`
# resolves correctly regardless of the working directory from which pytest
# is invoked (e.g. `python -m pytest backend/tests/ -v` from the repo root).
pythonpath = .
