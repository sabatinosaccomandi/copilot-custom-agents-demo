"""Test suite for the Flask application.

This package contains all automated tests.  Test modules should be placed
directly inside this package and named with the ``test_`` prefix so that
they are auto-discovered by pytest.

Modules
-------
conftest.py     Shared fixtures (app, client, users, tokens, products).
test_users.py   All /users/* endpoint tests.
test_products.py All /products/* endpoint tests.
"""
